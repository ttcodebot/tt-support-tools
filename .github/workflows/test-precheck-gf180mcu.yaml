name: Precheck Tests (gf180mcu)

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test-precheck:
    runs-on: ubuntu-latest
    env:
      PDK_ROOT: /home/runner/pdk
      PDK_VERSION: e8daeda73ca8f5814dbc0b11d1d05802251a3750

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        working-directory: precheck
        run: pip install -r requirements.txt

      - name: Install Gf180mcu PDK
        uses: TinyTapeout/ciel-action@v1
        with:
          pdk_name: gf180mcu
          pdk_version: ${{ env.PDK_VERSION }}
          pdk_root: ${{ env.PDK_ROOT }}

      - name: Install Nix
        uses: rikhuijzer/cache-install@v1.1.4
        with:
          key: nix-${{ hashFiles('precheck/default.nix') }}
          nix_file: 'precheck/default.nix'

      - name: Build Nix packages
        working-directory: precheck
        run: nix-build

      - name: Run tests
        working-directory: precheck
        run: nix-shell --run "PDK=gf180mcuD pytest --junit-xml=test_results.xml"
        
      - name: Test Summary
        uses: test-summary/action@v2.4
        with:
          paths: 'precheck/test_results.xml'
        if: always()
